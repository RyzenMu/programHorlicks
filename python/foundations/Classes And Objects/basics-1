# classes and objects in python
# classes and objects in python
class Person:
    def __init__(self, name, age, gender):
        self.name = name
        self.age = age
        self.gender = gender
        
p1 = Person("alex", 45, "male")

print(p1.name)
print(p1.age)
print(p1.gender)
