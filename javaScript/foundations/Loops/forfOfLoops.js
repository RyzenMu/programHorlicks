// for .. of loops iterate over data structures like array, strings, nodelists, maps

let arr = ['node', 'express', 'raect', 'typesccript'];

for (lan of arr) {
    console.log(lan);
}

let str = "Node Js"

for (letter of str ) 
{
    console.log(letter + " ");
}